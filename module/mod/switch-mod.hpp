/*
* Connect all ports with bridge in the node.
* Node -> Ports -> Switch
*/
/*
NetDeviceContainer BridgeConnect(Ptr<Node> node, NetDeviceContainer ports) {
  BridgeHelper bridge;
  bridge.Install(node, ports);
  return ;
}
*/
